#!/bin/zsh

# This file should be sourced
echo -n "Enter path to local repository > "
read path
cd "$path"

# echo -n "Enter repository URL > "
# read url
# git init
# git add .
# git commit -m "First commit"
# git remote add origin "$url"
# git remote -v
# git push -u origin master
