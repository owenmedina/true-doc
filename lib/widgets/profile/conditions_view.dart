import 'package:flutter/material.dart';

class ConditionsView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Conditions View to be completed')),
    );
  }
}